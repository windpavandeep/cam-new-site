; Increase limits for PDF uploads (dashboard).
; For local "php artisan serve", set these in php.ini or run:
;   php -d post_max_size=20M -d upload_max_filesize=15M artisan serve
upload_max_filesize = 15M
post_max_size = 20M
